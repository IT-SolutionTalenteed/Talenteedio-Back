# ============================================
# WORKFLOW AUTOMATIQUE - Variables d'environnement
# ============================================

# Email de contact administratif unique
# Tous les emails admin seront envoyés à cette adresse
ADMIN_CONTACT_EMAIL=contact@solutiontalentit.com

# Seuils de matching pour le workflow automatique
# Score >= AUTO_SEND_THRESHOLD : Envoi automatique au client
AUTO_SEND_THRESHOLD=80

# Score >= MANUAL_REVIEW_THRESHOLD et < AUTO_SEND_THRESHOLD : Validation manuelle
MANUAL_REVIEW_THRESHOLD=60

# Score < MANUAL_REVIEW_THRESHOLD : Rejet automatique

# ============================================
# Variables existantes requises
# ============================================

# Configuration SMTP (déjà présentes normalement)
MAILHOST=smtp.example.com
MAILPORT=587
MAILUSER=noreply@solutiontalentit.com
MAILPWD=your-mail-password

# URLs de l'application (déjà présentes normalement)
HOST=http://localhost:4000
FRONTEND_HOST=http://localhost:3000

# Base de données (déjà présente normalement)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your-db-password
DB_DATABASE=talenteed

# ============================================
# Notes
# ============================================

# 1. Copier ce fichier vers .env et remplir les valeurs
# 2. Ne jamais commiter le fichier .env
# 3. Vérifier que toutes les variables sont définies avant de démarrer
# 4. Les seuils peuvent être ajustés selon vos besoins

# Exemples de seuils alternatifs :
# - Plus strict : AUTO_SEND_THRESHOLD=85, MANUAL_REVIEW_THRESHOLD=70
# - Plus permissif : AUTO_SEND_THRESHOLD=75, MANUAL_REVIEW_THRESHOLD=50
